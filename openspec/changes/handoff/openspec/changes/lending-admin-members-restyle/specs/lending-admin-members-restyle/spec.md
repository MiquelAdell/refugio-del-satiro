# Spec delta — lending-admin-members-restyle

> This change **implements** the `lending-admin-members-restyle` capability with
> no behavioural change. The full requirements — `AdminMembersPage` uses the
> `ui/` primitives and tokens, wraps in `<PageLayout>`, and uses the `ui/Dialog`
> primitive for destructive-action confirmations, with **no** change to columns,
> actions, or endpoints — live in
> `openspec/specs/lending-admin-members-restyle/spec.md`. Implement those as
> written.
>
> **No spec delta:** this is a pure restyle. The canonical capability already
> captures the parity guarantees (same columns, same actions, same endpoints), so
> this change adds and modifies **no** requirements. If implementation surfaces a
> genuine behavioural question, add the delta here and update
> `openspec/specs/lending-admin-members-restyle/spec.md` accordingly.
